# 首个公测版 · 换机测试说明（1.0.0-beta1）

> 这份是**给测试者**的：装什么、按什么顺序验、看到什么算通过、出问题收什么信息。
> 同包里的 `README.md` 是给玩家看的（安装/卸载/回滚/配置说明），两者不冲突。
> `docs/beta-audit-first-public-beta.md` 是**内部审计报告**，别转给玩家。

---

## 0. 一句话流程

复制三个东西到游戏根目录 → 确认三个前提（版本一致 / 杀软白名单 / 单机）→ 启动看日志 →
进游戏按 `F4` → 按第 3 节的清单逐项点一遍 → 有问题就把 `logs\` 整个打包发回。

时间预算：冒烟 5 分钟，八个插件全过一遍约 30 分钟。

---

## 1. 装什么、放哪儿

把包里下列内容复制到**游戏根目录**（与 `GRW.exe` 同级）：

```
dinput8.dll                  加载器（代理 dinput8）
scripthook.ini               框架配置（已按本包 8 个插件写好 [plugins]；[forgemod] enabled=1 跟随开发机）
plugins\                     8 个插件，每个一个子文件夹
mods\                        Forge 侧载的模组内容（**两个包都带**；玩家版里 [forgemod] 默认 0，即默认关闭）
lang.example.ini             语言覆盖层参考模板（放着不用管）
LICENSE · THIRD-PARTY-NOTICES.txt
README.md                    框架功能与用法说明（同目录另有 README.txt 纯文本版）
docs\                        测试说明与内部审计报告（★ 本测试套件带；玩家版不带）
                             —— 每份都同时提供 .md 与 .txt；用记事本直接打开 .txt 即可
```

装完应该长这样：

```
Tom Clancy's Ghost Recon Wildlands\
├── GRW.exe
├── dinput8.dll
├── scripthook.ini
├── logs\                    ← 首次启动自动创建（日志都在这里）
├── mods\                    ← 本套件带：Forge 侧载的测试资源（见 4.1）
│   └── DataPC\                  3 个 .data 贴图条目 + 一份 README.txt
├── docs\                    ← 本套件带：beta-test-plan.md 与 beta-audit-….md
└── plugins\
    ├── skipintro\skipintro.asi
    ├── spawner\spawner.asi
    ├── firstperson\firstperson.asi
    ├── fov_changer\fov_changer.asi
    ├── cnchat\cnchat.asi
    ├── TimeWeatherControl\TimeWeatherControl.asi
    ├── OpticalCamo\OpticalCamo.asi
    └── ammo_capacity\ammo_capacity.asi
```

**不需要动任何游戏文件**；不需要管理员权限（装到 `Program Files` 下时日志会自动改落到游戏根目录，见第 6 节）。

---

## 2. 开测前的三个前提（能省掉九成「什么都没生效」）

### 2.1 游戏版本必须与开发机一致 ★ 最重要

框架里所有地址都是按开发机那份 `GRW.exe` 钉的。**版本不一致时，功能不会「部分工作」，而是基本全废。**

判据（启动一次后看日志）：`logs\` 里若出现下列字样，说明版本不匹配 —— 别继续测功能，直接把 `logs\` 发回来即可：

```
is not readable          （某处地址读不到，通常是常量过期）
stale constant?          （代码对不上，明确写了常量过期）
wanted 48 89 5C          （同上，括号里会给出实际字节）
MISMATCH                 （版本/指纹不一致）
```

**最快、最确定的判据是直接比指纹**（这一版框架是按 **UPlay 版**游戏钉的；Steam 版的 `GRW.exe` 与它不同）：

```powershell
# 在游戏根目录执行
(Get-Item '.\GRW.exe').Length
(Get-FileHash '.\GRW.exe' -Algorithm SHA256).Hash
```

开发机（也就是 `1.0.0-beta1` 所依据的那一份）：

| 项 | 值 |
|---|---|
| 大小 | `405,703,672` 字节（约 387 MB）|
| SHA-256 | `1284E4D5AAD5F1CA5D2832B6C46707B1D043205E929B5810FFE1CAF61CBDF751` |
| 文件日期 | 2026-09-15 23:43:55（UPlay 版，2026-09 游戏更新之后）|

> 这份 `GRW.exe` **没有版本资源**（`FileVersion` 为空），所以「体积 + SHA-256」就是它的版本号。
> 两者任一不符 → 先别测功能，把指纹和 `logs\` 一起发回来即可。

### 2.2 杀软白名单

`dinput8.dll` 是「代理 DLL + 挂钩」，Defender 之类的启发式规则很容易直接隔离它。
**先解压/复制之前，把游戏根目录加进白名单。**

怎么判断被隔离了：复制完 `dinput8.dll` 不见了；或游戏能开、但 `logs\` 里**什么文件都没有**（这说明 DLL 根本没被加载）。

### 2.3 只测单机

不要在有其他玩家的模式里带插件跑。插件已声明「幽灵战 / 雇佣兵」黑名单（框架会自动隐藏这些页），但测试阶段按单机来最省事。

---

## 3. 五分钟冒烟（装完先做这一步）

| # | 操作 | 通过的样子 |
|---|---|---|
| 1 | 启动游戏，等进到主菜单 | 游戏正常进入，**没有**弹窗、没有卡在片头 |
| 2 | 打开 `logs\scripthook.log` | 头两行是 `GRW ScriptHook 1.0.0-beta1` / `built <日期> <时间>` |
| 3 | 同一文件里搜 `loading plugin:` | **正好 8 行**，且每行下面跟一行 `loaded at ...` |
| 4 | 同一文件里搜 `no ... .asi` 或 `no plugin folders` | **一行都没有**（有就说明文件没放全） |
| 5 | 进游戏（进战役），按 **`F4`** | 出现模组菜单；左侧应有 8 个插件页 + `ScriptHook 设置` + `Forge 资源侧载` |
| 6 | 菜单最后一行切一次语言（中文 ↔ English） | **即时**全菜单换语言，不需要重启 |
| 7 | 进 `Forge 资源侧载` 页（本套件带 `mods\`） | 页面上显示已启用、能看到已加载的 mod；`logs\` 里与 Forge 相关的行**没有** `refused`（拒绝替换）字样 |

> 菜单缩放：如果这台机器的分辨率 / 显示器让菜单过大过小，可在 `scripthook.ini` 的 `[Settings]` 里加
> `MenuScale=1.2`（0 = 自动按分辨率缩放，默认就是自动）。UI 是自绘覆盖层，不吃系统 DPI 缩放。

---

## 4. 八个插件逐项

每项都按「怎么测 → 期望」写；结果填到第 9 节的表里。

| 插件 | 菜单页 | 怎么测 | 期望 |
|---|---|---|---|
| **skipintro** | 跳过片头 | 装好后**重启一次游戏**，注意看片头 | 四个片名（Nvidia / Ubisoft logo、光敏警示、存档警示）被跳过；进主菜单后状态行显示「已释放」 |
| **spawner** | 载具生成 | 生成一辆车 → 开一会儿 → 用「撤销」消掉 | 载具出现在附近；能上能开；撤销后消失。**顺带记录**：面朝北召唤时，车出现在**正前**还是**正右**（这是待确认项，见第 5 节）|
| **firstperson** | 第一人称 | 用热键切第一人称 → 走路、上车、开镜 | 视角进入第一人称；头部不遮挡；退出后视角正常 |
| **fov_changer** | 视野 | 拖「垂直视野」滑块 → 再点「恢复默认」 | 视野随之变化；恢复后回到游戏默认 |
| **cnchat** | 中文聊天 | 先在 `plugins\cnchat\cnchat.ini` 里打开开关 → 重启 → 游戏内按 `T` → 打中文 → `Enter` | 出现自绘输入框，能打中文（候选窗正常），回车后文字进游戏聊天框，`Esc` 取消 |
| **TimeWeatherControl** | 时间与天气 | 改「时间流速」→ 选一个天气 → 用「应用时间」跳到别的时段 | 时间流速改变、天气按选择变化、状态行显示 `时:分` + 时段 + 流速 |
| **OpticalCamo** | 光学迷彩 | 打开总开关 → 调「敌人视觉感知」档位（0.1x–0.9x）→ 到敌人附近 | 敌人感知明显变弱；关掉后恢复正常（关时会主动交还，线程 park） |
| **ammo_capacity** | 弹药容量 | 选一档倍率（0.50x–2.00x）→ 到弹药箱补给 | 弹匣容量按倍率变化；选 `1.00x` 时框架不装钩（日志会写 `not needed yet`） |

> `cnchat` 与 `OpticalCamo` **默认关闭**是设计（前者要在自己的 ini 里开，后者在菜单里开），不算故障。

### 4.1 Forge 资源侧载（框架功能，本套件带 `mods\`）

本套件里的 `mods\` 是一份**侦察直升机外观贴图替换**：`mods\DataPC\` 下三个
`.data` 条目，替换 `DataPC` 归档里 MTV Santa UNI Recon Heli 的外表面贴图
（`mods\README.txt` 是布局说明，**只有文件夹算数**，它本身不参与替换）。
`scripthook.ini` 里 `[forgemod] enabled=1` 已开（跟随开发机），所以**装好即生效**。

| 怎么测 | 期望 |
|---|---|
| 用 `spawner` 页召唤一架**侦察直升机**，靠近看机身贴图 | 是替换后的贴图（与不带 `mods\` 时不同）|
| 读一次档 / 快速旅行 / 换张地图后再看 | 替换**仍然生效**（不是只在第一张地图有效）|
| `F4` → `Forge 资源侧载` 页 | 显示已启用，能看到加载的条目 |
| 看 `logs\scripthook_forge.log` 与 `logs\scripthook_forge_io.log` | 有「找到并应用」的记录；**没有** `refused` 字样（唯一会被拒的原因写在 `mods\README.txt` 里：替换体积大于原条目 —— 这份资源是按原样做的，不该出现）|
| 想看关掉后的样子 | 把 `mods\DataPC` 改名为 `mods\~DataPC`（以 `~` 开头的文件夹被跳过），重启游戏 |

---

## 5. 本次修复的重点验证

这些是审计后改动过的地方，请特别留意（其余审核项见审计报告第六节）：

| # | 验什么 | 怎么验 | 期望 |
|---|---|---|---|
| 1 | **菜单文案即时切换 + 每帧翻译缓存** | 反复切中英文，进出各插件页 | 每次切换**立刻**全变；不出现一半中文一半英文 |
| 2 | **地面吸附 / 落点**（`physics` 等待改为事件驱动） | 在斜坡、台阶、桥面上生成载具与 NPC 类实体 | 落点正常，且**不再有明显卡顿**（原来最长可能卡近一分钟）|
| 3 | **速度类 API**（`havok` 体表加锁） | 生成载具后连续开、停、撞 | 无卡顿、无异常速度 |
| 4 | **配置解析**（短段名不再被当语言表丢掉） | 在 `scripthook.ini` 里加一个短段名（如 `[ui]`）与一条键，重启看是否生效 | 键被正常读到（此前会被静默丢弃）|
| 5 | **崩溃日志不会无限增长** | 连开几局（不必崩），看 `logs\scripthook_crash.log` | 超过 512 KB 会自动开新文件并留一行说明 |
| 6 | **日志落盘位置** | 正常装机看 `logs\`；若装到 `Program Files` 等只读位置 | 日志落到**游戏根目录**，且首行写明原因（不会无声无日志）|
| 7 | **`spawner` 落点方向**（已改为沿玩家朝向） | 面朝北召唤一辆；再转身朝东召唤一辆 | 两次都落在**正前方**（不再是「恒正东」）；只有拿不到相机时才退回旧的东向偏移 |
| 8 | **打包内容**（本条是自查） | 看本包目录 | `dinput8.dll`、`scripthook.ini`、8 个插件、`lang.example.ini`、许可与声明、**`mods\`**（Forge 侧载的测试资源，两个包都带）；测试包**另带** `docs\`；**没有** `plugins_off\`、`logs\`、`tools\` |
| 9 | **卸载** | 见第 8 节，删完后启动游戏 | 游戏正常；行为与未装框架时一致 |
| 10 | **Forge 侧载的稳定性**（审计第六节第 3 条） | 带着 `mods\` **正常游玩若干分钟**：读档两次、快速旅行/切地图、进出一两个据点，全程看直升机外观 | 外观替换一直生效；**不崩溃**；`logs\` 里与 Forge 相关的行无异常序列 |
| 11 | **菜单打开时不该出现系统鼠标指针** | 进游戏按 `F4` 开菜单，看鼠标所在位置；再开一次游戏自己的地图 / 暂停界面 | 模组菜单里指针**保持隐藏**（游戏原样）；游戏自己的界面里指针照旧正常出现 |
| 12 | **PvP 模式黑名单**（幽灵战 / 雇佣兵） | 进幽灵战或雇佣兵，按 `F4` 看菜单；顺带试一下插件功能（如弹药上限） | 除 `cnchat` 外 **7 个插件页消失**、功能停用（`cnchat` 声明了不限模式，属例外）；`logs\scripthook_blacklist.log` 出现一行 `in force now: Ghost War+MERCENARIES`（或对应模式） |

---

## 5.1 第二轮反馈的六项复测（2026-09-17 那台机器的六个问题）

这一版针对换机反馈的六条做了改动。请**按同样的顺序**再走一遍 ✓ —— 每条的判据都写在表里 ✓，出问题时对应的日志也列在「看哪里」一栏 ✓。

| # | 上次的现象 | 这次怎么测 | 通过的样子 | 看哪里 |
|---|---|---|---|---|
| 1 | 全新安装后**首次启动闪退**，之后正常 | 有机会时**重装游戏或清掉存档/配置**后再来一次首启（没条件就先跳过 ✓） | 首启不崩；即使覆盖层起不来，游戏也正常进、`F4` 无反应但**不崩** | `logs\scripthook_ovl.log`：应依次看到 `init thread: hwnd=…` → `settled 2000 ms` → `overlay: probe device …` → `hooks installed: … ok=1` → `imgui ready: …`；若某步后中断，那一行就是崩溃点 |
| 2 | 「各插件开关」页提示**反复上下闪动**、提示文字混进行里 | 打开该页，停留十几秒，上下翻几行 | 提示在顶部固定不动；行只有 8 个插件、每行一遍；不再出现「同一条提示当成行」 | `logs\scripthook_menu.log`：正常应只有 `menu diag: view …` 与 `item[…]`/`row[…]`；**若出现 `menu diag: ANOMALY …` 就是病根现行**，把那几行发回 |
| 3 | 从「菜单排序」返回后：**菜单语言**显示成页名、**页脚**乱、**根菜单标题消失** | 进「菜单排序」→ 返回 → 看「菜单语言」行、底部提示条、根菜单顶部标题 | 语言行显示语言名（`< 中文 >`）；页脚是提示文字；根菜单顶部有标题 | 同上（`scripthook_menu.log`）|
| 4 | 「时间 & 天气控制」页**各值乱显示**（全变成同一个数） | 进该页，把「小时」左右调到 9、「分钟」调成 30，再看四行流速 | 小时=9、分钟=30、四条流速各自显示自己的值（如 `1.00x`）；状态行显示 `时:分 时段 时间流速` | 同上（`menu diag: item[…]` 会逐行给出模型里的值与选项）|
| 5 | 第一人称玩久了**开镜过渡消失** | 先把 `plugins\firstperson\firstperson.ini` 设 `diag=1`，进游戏开第一人称，玩到出现该现象，**记下大致的发生时机** | 这个版本**只加诊断、未改行为**；目标是拿到数据 | `logs\firstperson.log`：`ads: 0 -> 1` / `1 -> 0` 的边沿行 + 每秒的 `beat: … ads=…`；把**现象发生前后**那几十行发回 |
| 6 | **光学迷彩**设 0.1x / 0.5x 都无效果 | 开开关、选 0.1x，**蹲下触发特效**，走到敌人附近看是否被秒发现 | 蹲下期间敌人感知明显变弱；不再「生效不到一秒就还原」 | `logs\OpticalCamo.log`：新增 `vote: N part(s), … -> active/inactive (latched …)` 行；**若它仍在 active/inactive 之间反复**，把这些行发回（那说明那个采样点本身在抖，要换判据）|

> 诊断是**有界**的：`scripthook_menu.log` 里的菜单快照整个会话最多 50 次 ✓（到时会写一行 `menu diag: stopping after 50 dumps` ✓），不会把磁盘写满 ✓。

### 5.1.1 现场结果（2026-09-17，开发机）

| # | 结果 |
|---|---|
| 2·3·4 | **已修复并复测通过** ✓ 一个根因：`ShLangText` 的查表缓存存的是**指针**，而未命中路径的 `Readable()` 每调用一次就轮换缓冲，于是同页多个未命中项全指向同几格内存（开关页多行显示同一标签/空行、时间天气页全成 `< 9 >`、语言行显示成页面名）。已改为缓存**持有副本**并返回副本。 |
| 6 | **已修复并实机确认** ✓ 那个逐部件 bit 每 ~2 秒在「全置位/全清零」之间干净翻转，因子因此每次只生效不到一秒；现对 `Active` 读数加 2 秒锁存，非游玩状态仍立即释放。使用者反馈：**蹲下能明显感觉到敌人不容易发现了** ✓ |
| 1 | 首启流程**完整走通、未再崩** ✓（`init thread` → `settled 2000 ms` → `subclass installed` → `probe device` → `hooks installed ok=1` → `imgui ready`）；崩溃日志中最后一条仍是 09-16 的旧记录（该文件是追加的）。**首次安装场景本身尚未再次复现验证** ✓ |
| 5 | **待数据**：`firstperson.log` 尚未生成（`diag=1` 未开）。 |
| — | `logs\scripthook_fpx.log` 里的 `headptr: tag 199 is out of range` **是有意的拒绝**，不是缺陷：199 = `0xC7` 是地图界面之后出现的旧布局读数，历史上正是它导致 4 次崩溃，代码已按设计跳过该帧。 |

### 5.1.2 下次复测记录

| # | 1 首启 | 2 开关页 | 3 返回后 | 4 时间天气 | 5 第一人称 | 6 光学迷彩 |
|---|---|---|---|---|---|---|
| 通过 | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |
| 备注 | | | | | | |

---

## 6. 已知的、不算 bug 的现象（避免误报）

1. **`logs\` 每次启动会被清空**（除了崩溃日志 `scripthook_crash.log`，它是追加的）—— 设计如此。
2. **崩掉的那一局**：崩溃报告写在 `logs\scripthook_crash.log`，内容只有模块名+偏移与基址，**不含你的用户名、机器名或完整安装路径**，可以放心发回来。
3. **`插件开关` 页里会出现你不认识的插件名**：框架会扫描 `plugins\` 下每个文件夹，并把缺行的自动补成 `=0`（列出了但其实没装、或不加载）。正常现象。
4. **幽灵战 / 雇佣兵模式下部分插件页自动消失**：这是插件自己声明的黑名单，设计如此。
5. **`ammo_capacity` 的 `lang.ini` 是空的注释模板**：它的文案编译在插件里，功能正常；那份文件只是留给「就地覆盖文案」用的。
6. **分辨率 / DPI**：UI 按 backbuffer 自动缩放，系统 DPI 缩放不影响；不合适就用 `MenuScale` 微调（见第 3 节）。
7. **`NPCSpawner` 不在本包里**：它功能只完成一半（生成的 NPC 不主动攻击），已按决定移出公测集，源码保留在仓库。
8. **Forge 在两个 PvP 模式下不看 mod**：幽灵战 / 雇佣兵下 `mods\` 一律不被服务（有意为之，日志会写 `mode gate: …`）。
9. **`mods\README.txt` 不参与替换**：Forge **只看文件夹、不看文件**，那份说明留着就行。
10. **`docs\` 只在测试套件里**：玩家版不带（内部资料）；`mods\` 则是**两个包都带**（Forge 侧载的测试资源，逐字节原样，不测就不加载）。
11. **模组菜单里没有鼠标指针**：本版有意**完全不碰**系统指针（菜单本来就靠键盘：`F4` / `↑↓` / `←→` / `Enter` / `ESC`），游戏自己的地图与暂停界面里指针照旧。

---

## 7. 出问题时怎么收集信息（很重要）

**默认就把这些发回来即可：**

1. 整个 `logs\` 目录（压缩成一个 zip）。
2. 这台机器上：游戏版本（`GRW.exe` 的属性 → 详细信息）、Windows 版本、显卡型号。
3. 出问题的那一刻你在做什么（哪个插件页、点了什么）。

**如果是崩溃：**

1. `logs\scripthook_crash.log` 最后一段（含 `=== GRW ScriptHook ...` 那一行起）。
2. 崩溃时是否开着别的覆盖层（MSI Afterburner / ReShade / 录屏等）。
3. 是否稳定复现，步骤越具体越好。

**如果「什么都没生效」（菜单按不出来）：**

1. 先确认 `dinput8.dll` 还在（防杀软隔离）。
2. 再确认 `logs\scripthook.log` 是否存在：
   - **不存在** → DLL 没被加载 = 文件位置不对或被隔离（第 2.1 / 2.2 节）；
   - **存在但不是 8 行 `loading plugin`** → 把该文件发回来。
3. 顺带看一下游戏根目录有没有多出 `scripthook.log`（说明 `logs\` 不可写，走了退路，见第 5 节第 6 项）。

---

## 8. 卸载与回滚

**卸载（删三个东西就干净了）：**

1. 关闭游戏。
2. 删除游戏根目录下的 `dinput8.dll`、`scripthook.ini`、`plugins\`。
3. 残留目录按需处理：
   - `logs\` —— 日志，可直接删；
   - `mods\` —— 你自己的 mod，与框架无关；
   - `plugins_off\<时间戳>\` —— 只有跑过开发构建才会有，里面是被移出公测集的插件（**是移走不是删除**）。
4. 删完启动游戏，行为与未装框架时一致。

**回滚到上一版**：本测试包是**全新安装**，删掉即是回滚。若以后在同一台机器上覆盖升级，
请在开发机构建时用 `-BackupDir` 备份（默认关闭），或自行保留一份 `dinput8.dll` + `plugins\`。

---

## 9. 结果记录表（填完发回）

冒烟：

| 项 | 结果 | 备注 |
|---|---|---|
| 游戏正常启动、片头被跳过 | ☐ 通过 ☐ 失败 | |
| `scripthook.log` 版本行 = `1.0.0-beta1` | ☐ 通过 ☐ 失败 | |
| 日志里正好 8 行 `loading plugin` | ☐ 通过 ☐ 失败 | |
| `F4` 出菜单、8 个插件页都在 | ☐ 通过 ☐ 失败 | |
| 语言切换即时生效 | ☐ 通过 ☐ 失败 | |

八个插件：

| 插件 | 能打开页面 | 功能正常 | 备注（含任何异常现象） |
|---|---|---|---|
| skipintro | ☐ | ☐ | |
| spawner | ☐ | ☐ | 落点在正前 / 正右：______ |
| firstperson | ☐ | ☐ | |
| fov_changer | ☐ | ☐ | |
| cnchat | ☐ | ☐ | |
| TimeWeatherControl | ☐ | ☐ | |
| OpticalCamo | ☐ | ☐ | |
| ammo_capacity | ☐ | ☐ | |

重点验证（第 5 节）：

| # | 项 | 结果 |
|---|---|---|
| 1 | 语言即时切换 | ☐ |
| 2 | 斜坡/台阶落点、无卡顿 | ☐ |
| 3 | 载具速度类操作无卡顿 | ☐ |
| 4 | 短段名配置键生效 | ☐ |
| 5 | 崩溃日志有大小上限 | ☐ |
| 6 | 日志落盘位置正确 | ☐ |
| 7 | spawner 落点方向已记录 | ☐ |
| 8 | 打包内容正确 | ☐ |
| 9 | 卸载后游戏正常 | ☐ |
| 10 | Forge 侧载持续生效、无崩溃 | ☐ |
| 11 | 菜单打开时指针保持隐藏、游戏自己界面仍有指针 | ☐ |
| 12 | PvP（幽灵战 / 雇佣兵）下插件页消失、功能停用 | ☐ |

**崩溃了？** 记录：次数 ____，可复现 ☐ 是 ☐ 否，步骤：______________________
