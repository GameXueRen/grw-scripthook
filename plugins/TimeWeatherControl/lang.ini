# TimeWeatherControl text: overwrite sample. The plugin ships its English and
# Chinese rows compiled in, so this file is only for the lines a player
# wants to change. Keys are the plugin's stable IDs - rewording the plugin
# never breaks these - and a section is a BCP-47 language code.
#
# Copy the plugin's own text into the section first, then edit it: a row
# here replaces the built-in text for that key in that language.
[zh-CN]
"@tw.page" = "时间 & 天气控制"
"@tw.w.hrain" = "风暴"
