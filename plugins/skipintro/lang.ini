# skipintro text overrides.
#
# The menu's own text is compiled in (kEn / kZh in skipintro.c) and
# already carries English and Chinese, so this file may stay empty: the
# menu reads with or without it. Add a row to change one line, or a whole
# [en-US] section to add a language.
#
#   [zh-CN]
#   "@skip.page" = 跳过开场动画
